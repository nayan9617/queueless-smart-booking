# QueueLess — Beta Incident Log

Record every real-user failure here. One row per incident.

| Date | User (role / alias) | Flow | Expected | Actual | Severity | Screenshot / log (`requestId`) | Root cause | Fix |
|------|---------------------|------|----------|--------|----------|--------------------------------|------------|-----|
|      |                     |      |          |        |          |                                |            |     |

Severity: `blocker` | `major` | `minor` | `ux`

Copy a row:

```text
Date:
User:
Flow:
Expected behavior:
Actual behavior:
Severity:
Screenshot/log (requestId / bookingId):
Root cause:
Fix:
```
